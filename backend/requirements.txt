# API runtime. Kept deliberately light: the app works fully on the
# sklearn bootstrap classifier without torch. Install ml/requirements.txt
# separately when you need to train or run the CNN.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7,<3.0
pydantic-settings>=2.4
email-validator>=2.1
sqlalchemy>=2.0
python-jose[cryptography]>=3.3
passlib>=1.7
argon2-cffi>=23.1
python-multipart>=0.0.9
celery>=5.4
redis>=5.0
numpy>=1.26
pillow>=10.3
scikit-learn>=1.4

# Testing
pytest>=8.0
httpx>=0.27
